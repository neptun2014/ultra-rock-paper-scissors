[project]
name = "ultra-rock-paper-scissors"
version = "0.1.0"
description = "A no-build rock-paper-scissors SPA."
requires-python = ">=3.10"

[dependency-groups]
dev = ["pytest>=8", "pytest-playwright>=0.5"]
